# Steps to create env
- uv init --python 3.10
- uv venv
- python -m ensurepip --upgrade (Should remove if not gonna use pip)
- uv add torch==2.6.0 --index https://download.pytorch.org/whl/cu118
- uv add -r requirements.txt
- uv add onnx (Export model to .onnx)
