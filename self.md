Clone the Original model repo
Rename remote with
```git remote rename origin upstream```

Create repo
Add that repo
```git remote add origin https://github.com/<your-username>/repo-name.git```

Commit and push with
```git push -u origin main```

